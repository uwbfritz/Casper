<header id="gh-head" class="site-header gh-head{{#match @custom.navigation_layout "Logo on cover"}} nav-cover{{/match}}{{#match @custom.navigation_layout "Logo in the middle"}} nav-center{{/match}}{{#match @custom.navigation_layout "Stacked"}} nav-stacked{{/match}}{{#match @custom.header_style "Left aligned"}} header-left{{/match}}{{#match @custom.header_style "Hidden"}} header-hidden{{/match}}" data-theme="{{#is "home"}}transparent{{else}}solid{{/is}}" data-theme-state="{{#is "home"}}transparent{{else}}solid{{/is}}">
    <div class="site-header__inner gh-head-inner{{#match @custom.navigation_layout "Logo in the middle"}} is-center{{/match}}{{#match @custom.navigation_layout "Stacked"}} is-stacked{{/match}}">
        <div class="site-brand gh-head-brand">
            <a class="site-logo gh-head-logo{{#unless @site.logo}} no-image{{/unless}}" href="{{@site.url}}">
                {{#if @site.logo}}
                    <img src="{{@site.logo}}" alt="{{@site.title}}" loading="lazy" decoding="async">
                {{else}}
                    <span class="footer-logo">{{@site.title}}</span>
                {{/if}}
            </a>
            <button class="gh-burger" aria-expanded="false" aria-controls="site-navigation" aria-label="Toggle navigation">
                <span></span>
                <span></span>
            </button>
        </div>

        <nav id="site-navigation" class="site-nav gh-head-menu{{#match @custom.header_style "Left aligned"}} is-left{{/match}}{{#match @custom.header_style "Hidden"}} is-hidden{{/match}}" aria-label="Primary"{{#match @custom.header_style "Hidden"}} aria-hidden="true"{{/match}}>
            {{navigation}}
        </nav>

        <div class="site-actions gh-head-actions">
            <button class="gh-search gh-icon-btn" aria-label="Search this site" data-ghost-search>
                {{> "icons/search"}}
            </button>
            <a class="btn btn-primary" href="{{@site.url}}/plan-your-visit/">Plan Your Visit</a>
            {{#if @site.members_enabled}}
                <div class="gh-head-members">
                    {{#unless @member}}
                        {{#unless @site.members_invite_only}}
                            <a class="btn btn-secondary" href="#/portal/signup" data-portal="signup">Join the Farm</a>
                            <a class="gh-head-link" href="#/portal/signin" data-portal="signin">Sign in</a>
                        {{else}}
                            <a class="btn btn-secondary" href="#/portal/signin" data-portal="signin">Members</a>
                        {{/unless}}
                    {{else}}
                        <a class="btn btn-secondary" href="#/portal/account" data-portal="account">Account</a>
                    {{/unless}}
                </div>
            {{/if}}
        </div>
    </div>
</header>
